{
  "_comment": "Etsy API v3 creds for reconcile.py auto-delist. x-api-key is sent as keystring:shared_secret (BOTH parts). Token needs scopes transactions_r (read sales) + listings_w (delist). Copy to etsy_credentials.json (git-ignored).",
  "client_id": "YOUR_ETSY_KEYSTRING",
  "shared_secret": "YOUR_ETSY_SHARED_SECRET",
  "refresh_token": "YOUR_ETSY_OAUTH_REFRESH_TOKEN",
  "shop_id": 12345678
}
